{
  "name": "Run Command",
  "description": "Execute a shell command and return results. Use for running terminal commands.",
  "tags": ["execute", "run", "command", "shell", "terminal", "bash", "syscall"],
  "nodes": [
    {
      "id": "run",
      "type": "unix",
      "config": {
        "command": "{{input.topic}}",
        "timeout": 30
      }
    }
  ],
  "edges": []
}

